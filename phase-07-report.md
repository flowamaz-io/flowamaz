# Phase Report — Flowamaz — Phase 07: Billing + Audit Log + Templates + Advanced Monitoring + Onboarding + Production Hardening

Generated: 2026-05-30 14:55
Generated by: PM Agent
Phase Type: Regular

---

## Summary

| Metric | Value |
|--------|-------|
| Total prompts in phase | 7 |
| Completed successfully | 7 |
| Required re-execution | 0 |
| Skipped (with reason) | 0 |
| Unresolved blockers | 1 (template runtime-format defect — see Verifier Critical) |

All 7 prompts landed on `develop` (commits `8cc80c7` → `6e752b9`). Build 0 errors / 0 warnings throughout; every prompt verified independently (build + unit + migration-on-Postgres + frontend typecheck) before commit.

---

## Phase Deliverables

- [x] 07-01 Billing + Stripe — checkout, customer portal, signature-verified idempotent webhooks, pricing + billing settings UI
- [x] 07-02 Audit Log — append-only `audit_events`, fire-and-forget AuditService, retention job, service-layer instrumentation, audit view + CSV export
- [x] 07-03 Workflow Templates — gallery, 10 official templates, install/publish, modals **(install→publish/run is BROKEN — see Critical)**
- [x] 07-04 Advanced Monitoring — per-node I/O snapshots, instance replay, Dev-only breakpoint surface, variable inspector
- [x] 07-05 Onboarding — 5-step product tour, contextual `?` tooltips, teaching empty states, welcome-email links
- [x] 07-06 Performance + Hardening — 7 production indexes, rich 5-dependency health checks, response caching, skeletons, full env validation, deployment checklist
- [x] 07-07 Integration — Phase7 integration tests, E2E S61–S72, coverage ≥80%, Trivy 0 Critical/High, PHASE_COMPLETE

---

## Prompt Execution Log

| Prompt | Status | Shallow Verify | Notes |
|--------|--------|---------------|-------|
| 07-01-billing-stripe | COMPLETE | PASS | Reused Plan/Subscription/Organisation/UsageAggregate; Stripe.net 51.2.0; migration `AddStripeBillingFields` |
| 07-02-audit-log | COMPLETE | PASS | Append-only entity; singleton fire-and-forget service; 9 services instrumented; migration `AddAuditEvents` |
| 07-03-workflow-templates | COMPLETE | PASS | Migration regenerated once to clear snapshot drift; **runtime-format defect found in phase review** |
| 07-04-advanced-monitoring | COMPLETE | PASS | WorkflowEvent snapshots + ParentInstanceId; reused existing IStepDebuggerService; breakpoint auto-pause not wired |
| 07-05-onboarding-improvements | COMPLETE | PASS | Frontend-heavy; reused preferences + FmEmptyState; welcome email updated |
| 07-06-performance-production-hardening | COMPLETE | PASS | Raw-SQL index migration (snapshot clean); external health probes non-critical + stubbed in Test |
| 07-07-phase7-integration | COMPLETE | PASS | Found + fixed real audit-CSV ObjectDisposedException bug; 1 live-Stripe-checkout test skipped |

---

## Verifier Deep Review

### Summary
Total issues: 8 | Critical: 1 | High: 2 | Medium: 3 | Low: 2

### Issues Found

```
ISSUE: Installed official templates cannot be published or executed (incompatible runtime YAML format)
FILE: backend/Flowamaz.Infrastructure/Persistence/Seed/WorkflowTemplateSeeder.cs (all seeded YAML, apiVersion: flowamaz/v1 + spec.nodes)
      vs backend/Flowamaz.Core/Workflow/SfgParser.cs:44-59 (RootDto reads root-level nodes/edges, no spec)
SEVERITY: Critical
FINDING: Two mutually incompatible workflow YAML schemas coexist. The template feature (07-03) validates and
  seeds templates in `flowamaz/v1` form (`spec.nodes`/`spec.edges`) via the 6-layer WorkflowValidator. But the
  RUNTIME graph parser SfgParser — used by WorkflowService publish (WorkflowService.cs:178) and by
  WorkflowOrchestrator.LoadGraphAsync — reads ROOT-LEVEL `nodes:`/`edges:` (confirmed: Phase-2 executing
  workflows use `workflow: {...}` + root `nodes:`). InstallTemplateAsync stores the template YAML as a Draft via
  WorkflowService.CreateAsync, which does NOT parse on create, so install succeeds; but publishing the installed
  workflow calls SfgParser.ParseAsync → SfgParseException("Workflow has no nodes"). Net effect: every workflow
  installed from an official template is a dead end (cannot publish, cannot run). The integration test missed it
  because it installs a hand-written root-level template, not a seeded official one. VERIFIED in code by PM.
FIX REQUIRED: Unify on one format. Preferred: make SfgParser/RootDto read `spec.nodes`/`spec.edges` (aligns the
  runtime with the documented flowamaz/v1 schema the validator + NL-generation already emit), OR re-author the 10
  seeded templates (and reconcile NlYamlGenerationService output) to root-level `nodes`. Add an integration test:
  install a seeded official template → publish → trigger one instance end-to-end.
```
```
ISSUE: New request DTOs lack FluentValidation; Stripe redirect URLs forwarded unvalidated (open-redirect surface)
FILE: backend/Flowamaz.Api/Controllers/BillingController.cs (CheckoutRequest/PortalRequest) → StripeService.cs:91-92,129
SEVERITY: High
FINDING: CLAUDE.md mandates FluentValidation on all request DTOs. None of the 6 new DTOs (Checkout, Portal,
  PublishTemplate, InstallTemplate, Replay, SetBreakpoint) have validators. CheckoutRequest.SuccessUrl/CancelUrl
  and PortalRequest.ReturnUrl are passed verbatim to Stripe with no allow-list/same-origin check.
FIX REQUIRED: Add validators; validate billing URLs against an allow-listed app origin (or require relative paths).
```
```
ISSUE: instance.started audit event records actor_type=user but no actor_user_id
FILE: backend/Flowamaz.Application/Workflow/Orchestrator/WorkflowOrchestrator.cs (~150-160)
SEVERITY: High
FINDING: Manual-trigger audit rows claim a human actor (actor_type=user + IP) but never set ActorUserId (TriggerAsync
  has no user id), undermining SOC2/ISO accountability and breaking the actor_id filter. Self-reported, confirmed.
FIX REQUIRED: Thread the triggering user id into TriggerAsync (or resolve from request context in AuditService),
  or set actor_type=system when no user id is available.
```
```
ISSUE: Breakpoint registry + dev endpoints exist but orchestrator never auto-pauses on a breakpoint (dead path)
FILE: backend/Flowamaz.Application/Workflow/Debugger/BreakpointRegistry.cs; WorkflowOrchestrator StepAsync
SEVERITY: Medium
FINDING: IsBreakpointSet is never consulted in StepAsync, so setting a breakpoint has no effect. Limited impact —
  the whole surface is hard-gated to Development. Self-reported, confirmed.
FIX REQUIRED: Consult IsBreakpointSet in StepAsync to hold the frontier (release via existing resume/step), or remove
  the registry/endpoints and document step-debugging as Redis-debugger-only.
```
```
ISSUE: PublishTemplateAsync throws plain InvalidOperationException (→ HTTP 500) for user-correctable conditions
FILE: backend/Flowamaz.Application/Library/Services/TemplateService.cs (~150-167); TemplatesController.cs
SEVERITY: Medium
FINDING: Missing name/category, workflow-not-found, not-Published, invalid YAML all throw bare
  InvalidOperationException → GlobalExceptionMiddleware maps to 500 (should be 400/404/422). Messages are actionable
  but the status codes are wrong. Self-reported, confirmed.
FIX REQUIRED: Throw a domain exception (TemplateException) carrying the correct status, like BillingException/AuditException.
```
```
ISSUE: Dev breakpoint/resume/step endpoints accept workspaceId without verifying membership
FILE: backend/Flowamaz.Api/Controllers/InstanceDebugController.cs (~56-97)
SEVERITY: Medium
FINDING: The /api/v1/dev/... endpoints are [Authorize] + Dev-gated but take workspaceId/WorkflowId from the request
  and pass them straight through without a membership/role check (unlike replay, which uses [RequireWorkspaceRole]).
  Acceptable in Development; a cross-workspace hole if the env gate were ever relaxed.
FIX REQUIRED: Add a workspace-membership/role check so authorization does not depend solely on the environment gate.
```
```
ISSUE: Replay endpoint returns bare NotFound() with no actionable body
FILE: backend/Flowamaz.Api/Controllers/InstanceDebugController.cs (~49-51)
SEVERITY: Low
FIX REQUIRED: Return an actionable message (instance not found in workspace / only terminal runs can be replayed).
```
```
ISSUE: Stray committed file ".env copy.example" (duplicate env template)
FILE: .env copy.example (tracked, root)
SEVERITY: Low
FINDING: A "copy" duplicate of .env.example was committed and updated with Stripe placeholders (values empty — no
  secret leak), creating a second source of truth that will drift.
FIX REQUIRED: Delete ".env copy.example"; keep Stripe vars only in .env.example.
```

### Deep Review Assessment
Production Ready: **NO** — solely due to the Critical template runtime-format defect (release-blocking for the template feature).
Notes: Verified-clean areas — Stripe secret/customer-id handling (never logged, never returned beyond a `HasBillingAccount` bool, never in webhook/audit payloads); webhook signature verified before processing with Redis idempotency; audit log genuinely append-only (AuditEvent is not a BaseEntity; only the retention job deletes); workspace/org isolation holds on all audit/replay/template queries; snapshots strip sensitive vars and never include stack traces; zero Vue `<style>` blocks; no "Something went wrong"/"No results" strings; EF reports no model drift; AuditService captive-dependency-free (singleton + IServiceScopeFactory). Rules 3/4/6 (AI metering / YamlDotNet / Git) N/A-clean.

---

## UX Review  (07-04, 07-05)

### Issues Found
```
ISSUE: Gates badge lacks a contextual ? tooltip (spec listed it)
SCREEN/FLOW: Sidebar "Gates" nav badge + GatesView
SEVERITY: Low
FINDING: Tooltips were added to status badge, health/empathy scores, co-pilot, api-keys, but not the Gates badge.
FIX REQUIRED: Add an FmTooltip ("Gates pause a workflow for a human decision; the badge shows approvals waiting on you.").
```
```
ISSUE: Gates empty state has no CTA (siblings do)
SCREEN/FLOW: GatesView empty state
SEVERITY: Low
FINDING: Teaches via description but omits cta-label (InstanceList/AuditLog use one). Not a violation; weaker parity.
FIX REQUIRED: Optional — add a CTA ("Learn about human gates" / "Go to Workflows").
```
```
ISSUE: Tour steps 4-5 spotlight the dashboard creation block while copy says "canvas"/"template"
SCREEN/FLOW: ProductTour steps 4-5
SEVERITY: Low
FINDING: The tour runs on the dashboard (no canvas present), so steps 4-5 anchor [data-tour="creation-methods"];
  the copy/visual mismatch is cosmetic. Functionally fine.
FIX REQUIRED: Reword steps 4-5 to match the highlighted element, or center-card those steps.
```
Verified working: 5-step tour, spotlight cutout, ●●○○○ dots, Escape=skip / ArrowRight=next, never-after-completion via `tour_completed_{workspaceId}`; StepDebugger expandable per-node I/O with green/amber/red/gray; ReplayModal pre-fills + navigates; VariableInspector polls 5s on running / static on terminal, masks sensitive.

---

## UI Review  (07-05)

### Issues Found
```
ISSUE: FmTooltip mouseleave/blur cannot keep bubble open for hover-selection
FILE: web/src/components/common/FmTooltip.vue (~56-60)
SEVERITY: Low
FINDING: No @mouseenter on the bubble; keyboard operability + aria roles are otherwise correct.
FIX REQUIRED: Optional — none required for compliance.
```
### Assessment
Style-block violations: **0** · TypeScript strict (no `any`): **PASS** · Tour spec compliance: **PASS** · Empty states teach: **PASS** · Cleared: **YES**
(Verified: `<script setup lang="ts">` throughout, aria roles on tooltip/skeleton/tour-dialog, loading/empty/error states present.)

---

## Testing Results

### Backend
Unit tests: **532 total | 532 passing | 0 failing** (target ≥530 met)
Integration tests: **111 total | 110 passing | 0 failing | 1 skipped** (live Stripe checkout — network; target ≥110 met)
Service-layer coverage (Phase 7 areas): StripeService **86.0%**, AuditService **88.7%**, TemplateService **100%**, ReplayService **93.1%**, HealthCheckService **100%**
Coverage acceptable (≥80%): **YES**

### Web
Unit + component tests (vitest): **58 total | 58 passing | 0 failing** (+5 ProductTour)
Typecheck: 0 errors · Lint: 0 errors
Coverage acceptable (≥80%): YES (service-layer areas)

### Mobile
N/A (Year 2)

### E2E (Playwright)
Authored: **S1–S72** (Phase 7 added S61–S72: billing, audit, templates, monitoring, onboarding, performance). Each new spec parses via `playwright test --list`. Not executed against a live full stack (no staging URL — consistent with prior phases).

### Load Tests (k6)
Not run — deferred to the production-readiness checklist (see below).

### Failing Tests
None.

---

## Security Scan

### Summary
Static — Critical: 0 | Warning: 0 | Info: 2
Trivy (`fs --scanners vuln --severity CRITICAL,HIGH ./backend`) — Critical: 0 | High: 0 (incl. new Stripe.net 51.2.0)
ZAP — SKIPPED (no staging URL)

### Focus-Area Verification (all PASS, evidence in code)
- **Stripe webhook signature** — PASS. `EventUtility.ConstructEvent` against STRIPE_WEBHOOK_SECRET (StripeEventVerifier); invalid sig → BillingException → 400; Redis processed-event-id idempotency (7d TTL); webhook reads RAW body (EnableBuffering + StreamReader), `[AllowAnonymous]`; secrets/customer ids never logged or returned.
- **Audit append-only** — PASS. Only INSERT + the server-side retention DELETE; grep confirms no Update/ExecuteUpdate/Remove on AuditEvents anywhere; controller is read/export-only. Secrets excluded from metadata (api-key: prefix+scopes only; webhook: workflowId only; billing: planId only).
- **Template YAML injection** — PASS. YamlDotNet into fixed POCOs (no type-resolving/tag deserialization → no gadget RCE); publish runs the 6-layer validator; install/publish Designer-gated + workspace-scoped.
- **Replay isTest** — PASS. IsTest=true hardcoded on every replay; terminal-only (else 409); Designer + workspace scope.
- **Breakpoints Dev-only** — PASS. All 5 dev endpoints early-return NotFound() when `!IsDevelopment()`.

### Findings
```
FINDING: Breakpoint dev-gate ordering — [Authorize] runs before the in-body IsDevelopment() guard, so production
  returns 401 (not 404). Functionality remains fully unreachable in production.
SEVERITY: Info
REMEDIATION: Optional — gate the whole controller via a Development-only route constraint for cleaner defense-in-depth.
```
```
FINDING: appsettings.json Stripe placeholders are empty strings bound from env at runtime — no secret committed.
SEVERITY: Info
REMEDIATION: None.
```

### Security Assessment
Cleared for next phase: **YES** — no Critical/High security findings. (The Verifier's open-redirect note on Stripe URLs is tracked as a High *correctness/validation* item below, not a confirmed exploit — billing endpoints are OrgOwner-gated.)
Blockers: None (security).

---

## Priority Classification — For Chat Fix Phase Generation

### Must Fix Before Next Phase (Critical + High)
1. **[Verifier] WorkflowTemplateSeeder / SfgParser** — Critical: installed official templates use `flowamaz/v1` `spec.nodes` but the runtime SfgParser reads root-level `nodes`, so installed templates cannot publish or run. Unify the format + add an install→publish→trigger integration test.
2. **[Verifier] BillingController / StripeService** — High: add FluentValidation to the 6 new request DTOs; allow-list/same-origin-validate the Stripe success/cancel/return URLs (open-redirect surface).
3. **[Verifier] WorkflowOrchestrator audit** — High: populate `actor_user_id` for manual-trigger `instance.started` events (or mark actor_type=system).

### Travel Forward (Medium + Low)
1. [Verifier] Breakpoint auto-pause not wired into StepAsync (Dev-gated dead path) — wire it or remove the surface.
2. [Verifier] TemplateService publish throws InvalidOperationException → 500; should be a domain exception with 400/404/422.
3. [Verifier] Dev debug endpoints lack workspace-membership check (Dev-gated; defense-in-depth).
4. [Verifier] Replay endpoint returns bare NotFound() — add actionable body.
5. [Verifier] Delete stray `.env copy.example`.
6. [UX] Add Gates-badge tooltip + Gates empty-state CTA.
7. [UX] Reword tour steps 4-5 (spotlight/copy mismatch).
8. [UI] FmTooltip hover-keep-open (optional).
9. [Security] Optional: route-level Dev gate for breakpoint controller (defense-in-depth).

---

## Next Phase Readiness

Fix phase required: **YES**
Critical + High issues requiring fix phase: **3** (1 Critical + 2 High)
Blockers preventing next phase: **1** — the template runtime-format Critical makes the 07-03 template feature non-functional end-to-end. Everything else is green (build 0/0, unit 532, integration 110, security cleared, coverage ≥80%, Trivy clean).

---

## PM Suggested Next Scope

Fix phase (fix-07) should address:
1. **Unify workflow YAML format** — make SfgParser/RootDto read `spec.nodes`/`spec.edges` (recommended) OR re-author the 10 seeded templates + reconcile NlYamlGenerationService; add an end-to-end install→publish→trigger test for an official template.
2. **DTO validation + Stripe redirect-URL allow-listing** across the 6 new request DTOs.
3. **Audit actor_user_id** on manual-trigger events.
4. Cleanups: TemplateService domain exception (status codes), breakpoint auto-pause decision, dev-endpoint workspace check, replay actionable 404, remove `.env copy.example`, Gates tooltip/CTA, tour step 4-5 copy.

## Production Readiness Checklist (from 07-07 — before public launch)
1. Stripe account in live mode (currently test mode) + real STRIPE_* price/secret/webhook envs.
2. SSL certificate for the production domain.
3. GITHUB_CONNECTORS_TOKEN set for community submissions.
4. AWS Lightsail server provisioned.
5. Branch protection rules enabled on `develop`/`main`.
6. Staging environment + ZAP scan.
7. k6 load test on the critical endpoints (trigger, status-poll, login, co-pilot).

---

*Upload this file to ClaudeCode Foundation chat for review and next phase generation.*

# Phase Report — Flowamaz — Phase 02: Workflow Engine — Durable Execution + Observability

Generated: 2026-05-26
Generated by: PM Agent
Phase Type: Regular

---

## Summary

| Metric | Value |
|--------|-------|
| Total prompts in phase | 8 |
| Completed successfully | 8 |
| Required re-execution | 0 |
| Skipped (with reason) | 0 |
| Unresolved blockers | 0 |

Backend: `dotnet build` 0 errors / 0 warnings · **183 unit + 56 integration tests pass**.
Web: `npm run build` 0 TypeScript errors · **13 vitest pass**.

---

## Phase Deliverables

- [x] 02-01 Workflow & instance entities, append-only event log, SKIP-LOCKED worker claim, startup migration
- [x] 02-02 SFG YAML parser, durable-execution orchestrator, Redis task queue, worker, delayed-queue promoter
- [x] 02-03 HTTP action worker, node-worker registry, saga engine (Backward/Forward/Pivot), lease-expiry + gate-timeout jobs
- [x] 02-04 Workflow / instance / gate REST API + instance-status WebSocket
- [x] 02-05 Workflow Interpreter (CEO/Auditor/Developer), run timeline, step debugger
- [x] 02-06 Vue views: workflow + instance lists/details, run-timeline, interpreter panel, live WebSocket
- [x] 02-07 Process Intelligence (hourly metrics + insights), Workflow Weather, dashboard wired to real data
- [x] 02-08 Phase-2 integration tests, end-to-end loop wiring, Playwright S18–S25, PHASE_COMPLETE

---

## Prompt Execution Log

| Prompt | Status | Shallow Verify | Notes |
|--------|--------|---------------|-------|
| 02-01 | COMPLETE | PASS | Travel-forward startup migration delivered; Docker self-bootstraps schema |
| 02-02 | COMPLETE | PASS | RPOPLPUSH claim (SE.Redis has no blocking BRPOPLPUSH) |
| 02-03 | COMPLETE | PASS | Node executors built; full loop wiring deferred → completed in 02-08 |
| 02-04 | COMPLETE | PASS | Concrete app services (AuthService precedent); WS polling push |
| 02-05 | COMPLETE | PASS | Local AI completion seam; debugger Dev/Staging gate |
| 02-06 | COMPLETE | PASS | Zero style blocks; TS strict |
| 02-07 | COMPLETE | PASS | Quartz job thin-wraps Application service |
| 02-08 | COMPLETE | PASS | Wired FailNode→Saga + worker node execution; integration tests green |

---

## Verifier Deep Review

### Summary
Total issues: 5 | Critical: 0 | High: 0 | Medium: 3 | Low: 2

### Issues Found

```
ISSUE: Sensitive workflow variables are masked in API responses but not encrypted at rest
FILE: backend/Flowamaz.Core/Entities/Workflow/WorkflowVariable.cs
SEVERITY: Medium
FINDING: IsSensitive drives "***" masking on read; the stored value is plaintext jsonb.
FIX REQUIRED: Encrypt sensitive values at rest (credential vault) — scheduled for Phase 4.
```

```
ISSUE: AI completion is a deterministic local stub, not a real provider call
FILE: backend/Flowamaz.Infrastructure/Services/AiCompletionService.cs
SEVERITY: Medium
FINDING: CEO narrative + PI insights resolve F5 and meter tokens but the provider SDK call is not wired.
FIX REQUIRED: Wire Anthropic.SDK behind IAiCompletionService when a platform key is configured.
```

```
ISSUE: Per-workflow SLA threshold has no storage source
FILE: backend/Flowamaz.Application/Analytics/ProcessIntelligenceService.cs
SEVERITY: Medium
FINDING: WorkflowMetric.SlaThresholdMs is always null at runtime, so SLA-risk insights never fire live.
FIX REQUIRED: Add an SLA setting to the workflow definition / workspace settings.
```

```
ISSUE: Instance list live-updates open one WebSocket per non-terminal instance (cap 20)
FILE: web/src/views/instance/InstanceListView.vue
SEVERITY: Low
FINDING: Works but does not scale to large lists; a workspace-level stream would be better.
FIX REQUIRED: Add a single multiplexed workspace stream (future).
```

```
ISSUE: Narrative download returns markdown, not PDF
FILE: backend/Flowamaz.Api/Controllers/WorkflowInterpreterController.cs
SEVERITY: Low
FINDING: "keep it simple for Phase 2" — markdown attachment instead of rendered PDF.
FIX REQUIRED: Add QuestPDF/HTML-to-PDF rendering (future).
```

### Deep Review Assessment
Production Ready: YES (for an internal/beta workflow engine) — no Critical/High correctness issues.
Notes: Clean-architecture boundaries held (Api→Application→Core; Quartz/Redis stay in Infrastructure).
Workspace isolation enforced on every scoped query. Event log is insert-only. Idempotent triggers.

---

## UX Review

(02-06 frontend)

```
ISSUE: No in-app "create workflow" path yet
SCREEN/FLOW: /workflows empty state
SEVERITY: Low
FINDING: Empty state previews the six creation methods (greyed) — expected, creation is Phase 3.
FIX REQUIRED: None for Phase 2; Phase 3 delivers the SFG canvas + creation methods.
```
Loading / empty / error states present on every new list and detail view (FmSpinner / FmEmptyState / FmErrorState). Live "Live · N / Reconnecting…" indicator on instance views.

---

## UI Review

(02-06 frontend)

```
ISSUE: none blocking
FILE: web/src/components/workflow/*, web/src/views/{workflow,instance}/*
SEVERITY: Low
FINDING: Zero <style> blocks (verified); Tailwind-only; status colours computed (health bands, status badges).
FIX REQUIRED: None.
```

---

## Testing Results

### Backend
Unit tests: 183 total | 183 passing | 0 failing
Integration tests: 56 total | 56 passing | 0 failing
Service layer coverage (unit ∪ integration, Phase 2 areas):
- Application/Analytics — **88%** ✓
- Application/Workflow (+ Core SfgParser) — **73%**
- Infrastructure/Jobs — **20%**
- Infrastructure/Workers — **0%**
- Phase 2 overall — **77%**
Coverage acceptable (>=80%): **NO** — see "Must Fix" below (background components + some service read paths).

### Web
Unit + component tests: 13 total | 13 passing | 0 failing
Build: 0 TypeScript errors.
Coverage: not separately gated this phase (component/unit tests cover FmRunTimeline + useInstanceWebSocket).

### E2E (Playwright)
Scenarios written: S18–S25 (8). Not executed in this build session — they require a live full
stack (backend + web + db + redis, worker enabled, platform key for CEO). To run in Docker/CI.

### Failing Tests
None.

---

## Security Scan

### Summary
Static — Critical: 0 | Warning: 0 | Info: 1
Trivy — see below (HIGH/CRITICAL on built images)
ZAP — SKIPPED (no staging URL, per CLAUDE.md)

### Static review (Phase 2 diff)
- No AI API keys or secrets committed in source/config (only empty placeholders + a test-only env key).
- WebSocket: JWT validated AND workspace membership checked BEFORE `AcceptWebSocketAsync` (handler lines 55/65/76).
- Step debugger: Production guard enforced at the service layer (`StepDebuggerService.LoadGuardedAsync` → 403).
- Sensitive variables: masked "***" in all API responses (InstanceService). **Info:** not yet encrypted at rest (Phase 4).
- Workspace isolation: every workflow/instance/analytics query is workspace-scoped.

### Trivy
`trivy image --severity HIGH,CRITICAL` (v0.70.0):
- `flowamaz-backend:latest` — **CRITICAL 0 · HIGH 0**
- `flowamaz-web:latest` — **CRITICAL 0 · HIGH 0**

Caveat: images were built during the fix-01 Docker smoke (before the Phase 2 NuGet additions).
Rebuild + rescan in CI to cover the Quartz / YamlDotNet dependency trees on the published image.

### Security Assessment
Cleared for next phase: YES (no Critical/High code findings). Encryption-at-rest for sensitive
variables is the one security item to carry forward (Phase 4 credential vault).

---

## Priority Classification — For Chat Fix Phase Generation

### Must Fix Before Next Phase (Critical + High)
1. [Testing] Infrastructure/Workers (OrchestratorWorker 0%) + Infrastructure/Jobs (20%) — add unit tests (the background worker is disabled in the integration host to avoid races) to clear the ≥80% gate.
2. [Testing] Application/Workflow services 73% — cover InstanceService read paths + orchestrator Complete/Cancel/UpsertVariable.

### Travel Forward (Medium + Low)
1. [Verifier] Encrypt sensitive WorkflowVariable values at rest (Phase 4 credential vault).
2. [Verifier] Wire a real provider call behind IAiCompletionService (currently a deterministic local stub).
3. [Verifier] Add a per-workflow SLA threshold source so SLA-risk insights fire live.
4. [Verifier/UX] Single workspace-level WebSocket stream instead of one socket per instance.
5. [Verifier] Real PDF narrative download.
6. [Testing] Execute Playwright S18–S25 against a live stack in CI.

---

## Next Phase Readiness

Fix phase required: YES (small) — primarily to raise Phase 2 coverage to ≥80% on the worker/jobs
and workflow-service read paths, plus the travel-forward items above as scoped.
Critical + High issues requiring fix phase: 0 correctness; 2 coverage items.
Blockers preventing next phase: none.

## PM Suggested Next Scope

fix-02 should address:
1. Unit tests for OrchestratorWorker (`ProcessInstanceAsync`/`ExecuteNodesAsync` with mocked scope), DelayedQueuePromoterJob, GateTimeoutJob, ProcessIntelligenceJob.
2. Service tests for InstanceService (detail/events/variables/retry/timeline) + orchestrator CompleteNode/Cancel.
3. Run Playwright S18–S25 in the Docker/CI stack.

Then Phase 03 — SFG Canvas + all creation methods + magic features.
